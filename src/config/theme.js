// Theme Configuration
// Update these colors to match your Squarespace theme

export const theme = {
  colors: {
    // Primary colors
    primary: "#4A90E2",        // Main brand color
    primaryDark: "#2C5F8D",    // Darker shade of primary
    primaryLight: "#B8D4E3",   // Lighter shade of primary
    
    // Background colors
    background: "#FFFFFF",      // Main background
    backgroundLight: "#F8F9FA", // Light background
    backgroundDark: "#1A1A1A",  // Dark background (for contact)
    
    // Text colors
    textPrimary: "#2C3E50",     // Main text color
    textSecondary: "#8B949E",   // Secondary text
    textLight: "#FFFFFF",      // Light text (on dark backgrounds)
    
    // Accent colors
    accent: "#E8F4F8",         // Accent color
    accentSoft: "#B8D4E3",     // Soft accent
    
    // Glassmorphism
    glassBg: "rgba(255, 255, 255, 0.6)",  // Glass background
    glassBorder: "rgba(184, 212, 227, 0.5)", // Glass border
    glassBlur: "blur(10px)",   // Blur effect
  },
  
  // Typography
  fonts: {
    primary: "Poppins, sans-serif",
    heading: "Playfair Display, serif", // Optional elegant font
  },
  
  // Spacing
  spacing: {
    small: "0.5rem",
    medium: "1rem",
    large: "2rem",
    xlarge: "3rem",
  },
  
  // Border radius
  borderRadius: {
    small: "8px",
    medium: "12px",
    large: "20px",
    xlarge: "24px",
  },
  
  // Shadows
  shadows: {
    small: "0 2px 8px rgba(0, 0, 0, 0.1)",
    medium: "0 4px 16px rgba(0, 0, 0, 0.12)",
    large: "0 8px 24px rgba(0, 0, 0, 0.15)",
  },
};

export default theme;

